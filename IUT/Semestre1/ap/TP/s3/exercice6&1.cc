#include<iostream>
#include<string>
using namespace std;


int main() {
  float x, y;
  cout << "valeur de 'x' ? "; cin >> x;
  cout << "valeur de 'y' ? "; cin >> y;
  if (x>y)
    cout << "'y' est le minimum" << endl;
  else
    cout << "'x' est le minimum" << endl;

return 0; }

